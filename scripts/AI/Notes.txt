-------Changes------
When making new models or chaning event types 


------Training Data-----

  --Events
    Goal keeper is same as stat bomb and is for save, punch, smother, claim/collection, goalkeeper pickup, penalty faced) (no backpass thou thats still a pass)


-------MODELS--------------
  --General

    Linera regresion Models (LSTM not worth it for Improvments)
    All on T+1 right now but WILL change this later

    ---Improvments
      More lag data 
      Output of player?
      Context (Time left, scrore)
      The ~2000 more files
      Dead Ball input for training
      Dead bal output model
      More sub-event types (split miss, shot on taget and goal to also include close, medium and long using distance)
      Use carry event (would requrie structual change (unless also made sub event??))

        ------Solution to pre match incacuracy (bad at guesing without lag data) (Would have to query something things)
          --Prev records?
          --Teams Ueafa co-ofecient? 
      

    ------

    Not normilized perspective (H/A on seperate models) as 
    this helps to predict stats before lag data can be used

    Uses T, T-5 & T - 20 for lags but -5 & - 20 are abitrary picks

    does not use carry as a metric to train as it's to regular and to difficult as to determine the value without alos taking things like distance 

  ---PASS---

      Average Pass Deltav is 9.7
      Pass Model MAE(Mean Absolute Error) is ~4.3 (44%)

    